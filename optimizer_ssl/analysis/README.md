# Analysis package

This package is reserved for cleaned, importable paper-analysis code.

Training-time spectral telemetry already lives in `optimizer_ssl/spectra/`. This folder will contain post-processing logic after the cleaned plotting/table scripts are integrated.

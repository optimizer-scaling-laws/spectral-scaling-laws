# Examples

CPU-safe examples for using the spectral diagnostics outside the training stack.

```bash
python examples/probe_synthetic_activations.py
python examples/probe_your_model.py
```

These examples do not reproduce the paper-scale experiments; they demonstrate the
model-agnostic metric API.
